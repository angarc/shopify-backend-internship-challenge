<h1 class='page-title'>Add New Photo</h1>

<hr/>

<%= simple_form_for @photo, url: dashboard_photos_path do |f| %>
  <%= render 'form', f: f %>

  <%= f.submit 'Save Photo', class: 'btn btn-primary' %>
<% end %>
