<div class='container'>
  <h1><%= @photo.title %></h1>
  <div class='row'>
    <div class='col-lg-8'>
      <%= image_tag url_for(@photo.image), width: '100%' %>

      <p class='mt-3'><%= @photo.description %></p>
    </div>
  </div>
</div>
